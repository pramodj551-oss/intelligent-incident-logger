# -- LLM Providers (install only what you need) -------------------------------
groq>=0.11.0             # Groq Cloud -- FREE  (LLM_PROVIDER=groq)
openai>=1.40.0           # OpenAI              (LLM_PROVIDER=openai or EMBEDDING_MODE=openai)
instructor>=1.4.0        # Pydantic structured outputs (Groq + OpenAI + HuggingFace)

# HuggingFace uses the openai SDK with a custom base_url -- no extra package needed
# HUGGINGFACE_API_KEY only required if LLM_PROVIDER=huggingface

# -- Agent Framework ----------------------------------------------------------
langgraph>=0.2.0

# -- Vector Store & Embeddings ------------------------------------------------
chromadb>=0.5.0
langchain-text-splitters>=0.2.0

# Local embeddings (EMBEDDING_MODE=local, the default)
sentence-transformers>=2.7.0

# -- API Backend --------------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# -- Frontend -----------------------------------------------------------------
streamlit>=1.35.0

# -- Utilities ----------------------------------------------------------------
pydantic>=2.7.0
python-dotenv>=1.0.0
